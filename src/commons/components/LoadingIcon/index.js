import React from "react";
import { IconLoading } from "../Icon";

const LoadingIcon = () => {
  return (
    <div className="sds-load__icon sds-input__icon-static">
      <IconLoading />
    </div>
  );
};

export default LoadingIcon;
